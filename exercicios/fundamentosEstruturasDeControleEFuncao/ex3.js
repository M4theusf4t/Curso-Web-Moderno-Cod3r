/**
 * Crie uma função que recebe dois parâmetros, base e expoente, e 
 * retorne a base elevada ao expoente.
 */

function potencia(x, y) {
    return console.log(`Resultado: ${Math.pow(x, y)}`);
}
potencia(24, 2);
potencia(2, 2);